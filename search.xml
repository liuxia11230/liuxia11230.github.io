<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>网络科技2</title>
    <url>/2019/10/11/title-1/</url>
    <content><![CDATA[<!-- toc -->

<h3 id="闲话"><a href="#闲话" class="headerlink" title="闲话"></a>闲话</h3><p><code>Live Photos</code> 是 iOS 9 的一个新特性，它提供相对于传统照片更生动的展现方式。当用户按下拍照键<br>的时候，照片 APP 会捕捉比传统照片更多的内容，包括声音和更多的帧。当用户浏览这些照片时，他们能与这些照片交互并且回放那些被捕捉的内容，这样让照片看起来更加生动。</p>
<p>iOS 9.1 提供了与 <code>Live Photos</code> 交互的 API，还可以将这些数据导出，用作分享。新提供的 <code>Photos framework</code> 可以从 <code>PHImageManager</code> 对象中取得 <code>PHLivePhoto</code> 的实例。这个对象可以用来表示 <code>Live Photo</code> 中的数据。你可以使用 <code>PHLivePhotoView</code> 对象来现实 <code>Live Photo</code> 的内容。<code>PHLivePhotoView</code> 用于展示图片，处理用户交互，还有对内容显示的处理。</p>
<p>你还可以使用 <code>PHAssetResource</code> 来访问 <code>PHLivePhoto</code> 来进行共享操作。你可以使用 <code>PHImageManager</code> 或 <code>UIImagePickerController</code> 来从用户的照片库中请求 <code>PHLivePhoto</code> 对象。如果你还在开发一个 Extension，你还可以使用 <code>NSItemProvider</code> 对象来获取 <code>PHLivePhoto</code> 对象。在接受共享的一方，你可以通过发送过来的文件，来重建 <code>PHLivePhoto</code>对象。</p>
<a id="more"></a>

<!-- toc -->

<h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><p><code>PHLivePhotoView</code>需要导入<code>PhotosUI</code></p>
]]></content>
      <categories>
        <category>it</category>
      </categories>
      <tags>
        <tag>iOS</tag>
      </tags>
  </entry>
  <entry>
    <title>网络科技</title>
    <url>/2019/10/11/title/</url>
    <content><![CDATA[<!-- toc -->

<h3 id="闲话"><a href="#闲话" class="headerlink" title="闲话"></a>闲话</h3><p><code>Live Photos</code> 是 iOS 9 的一个新特性，它提供相对于传统照片更生动的展现方式。当用户按下拍照键<br>的时候，照片 APP 会捕捉比传统照片更多的内容，包括声音和更多的帧。当用户浏览这些照片时，他们能与这些照片交互并且回放那些被捕捉的内容，这样让照片看起来更加生动。</p>
<p>iOS 9.1 提供了与 <code>Live Photos</code> 交互的 API，还可以将这些数据导出，用作分享。新提供的 <code>Photos framework</code> 可以从 <code>PHImageManager</code> 对象中取得 <code>PHLivePhoto</code> 的实例。这个对象可以用来表示 <code>Live Photo</code> 中的数据。你可以使用 <code>PHLivePhotoView</code> 对象来现实 <code>Live Photo</code> 的内容。<code>PHLivePhotoView</code> 用于展示图片，处理用户交互，还有对内容显示的处理。</p>
<p>你还可以使用 <code>PHAssetResource</code> 来访问 <code>PHLivePhoto</code> 来进行共享操作。你可以使用 <code>PHImageManager</code> 或 <code>UIImagePickerController</code> 来从用户的照片库中请求 <code>PHLivePhoto</code> 对象。如果你还在开发一个 Extension，你还可以使用 <code>NSItemProvider</code> 对象来获取 <code>PHLivePhoto</code> 对象。在接受共享的一方，你可以通过发送过来的文件，来重建 <code>PHLivePhoto</code>对象。</p>
<a id="more"></a>

<!-- toc -->

<h3 id="准备工作"><a href="#准备工作" class="headerlink" title="准备工作"></a>准备工作</h3><p><code>PHLivePhotoView</code>需要导入<code>PhotosUI</code></p>
]]></content>
      <categories>
        <category>xxx</category>
      </categories>
      <tags>
        <tag>iOS</tag>
      </tags>
  </entry>
</search>
